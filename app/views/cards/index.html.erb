<div class="col-xs-12 col-md-6 col-md-offset-3">
  <% if @cards.empty? %>
    <h3>You have no cards left today!</h3>
  <% else %>
    <%= render partial: 'card', locals: { card: @cards.first } %>
  <% end %>
</div>