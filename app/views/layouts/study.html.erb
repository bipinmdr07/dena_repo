<!DOCTYPE html>
<html>
<head>

  <%= render '/layouts/meta' %>

  <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.1.0/styles/railscasts.min.css">
  <script src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.1.0/highlight.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/9.1.0/languages/erb.min.js"></script>

</head>

<body id="page-top" class="index course_html">

  <div id = "wrap">

    <%= render '/layouts/nav_lessons' %>

    <div class="container">
      <div class = "content">

        <div class="row">
          <div class="col-xs-12">
            <ul class="nav nav-tabs">
              <li role="presentation"><%= link_to 'Home', study_path %></li>
              <li role="presentation"><%= link_to 'Your Cards', cards_path %></li>
              <li role="presentation"><%= link_to 'Master Decks', decks_path %></li>
            </ul>
          </div><!-- col-xs-12 -->

          <br>

          <div class="col-xs-12">
            <%= yield %>
          </div><!-- col-xs-9 -->
        </div><!-- row -->
      </div><!-- content -->
    </div><!-- container-fluid -->

  </div>

  <%= render '/layouts/lesson_footer' %>

  <%= render '/layouts/audio' %>

  <script type="text/javascript">
  ready = function() {
    $('pre code').each(function(i, e) {hljs.highlightBlock(e)});
    var url = window.location;
    // Will only work if string in href matches with location
    $('ul.nav a[href="' + url + '"]').parent().addClass('active');
    // Will also work for relative and absolute hrefs
    $('ul.nav a').filter(function() {
        return this.href == url;
    }).parent().addClass('active');
  }
  $(document).ready(ready);
  $(document).on('page:load', ready);
  $(document).on('page:change',  function() {
    $('pre code').each(function(i, e) {hljs.highlightBlock(e)});
  });
  $(document).on('page:restore', function() {
    $('pre code').each(function(i, e) {hljs.highlightBlock(e)});
  });
  $(document).ready(function() {
    $('#post_body').summernote();
  });

  </script>
</body>
</html>
