<ul class="deck-list">
	<% @decks.each do |deck| %>
		<li>
			<span class="deck-title"><%= link_to deck.title, admin_deck_path(deck) %></span>
			<span class="deck-card-count"><%= deck.cards.count %></span>
		</li>
	<% end %>
</ul>
