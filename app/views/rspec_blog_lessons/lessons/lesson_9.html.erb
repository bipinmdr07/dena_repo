

<p>
  Before we get into testing, let's set up our <code>Post</code> model and controller.
</p>

<h2>Instructions</h2>
<ul>
  <li>Generate a <code>Posts</code> controller</li>
  <li>Generate a <code>Post</code> model</li>
  <li>Add the following columns for the <code>Post</code> model in the <code>create_posts</code> migration file</li>
    <ul>
      <li>A <code>string</code> field called <code>title</code></li>
      <li>A <code>string</code> field called <code>author</code></li>
      <li>A <code>text</code> field called <code>content</code></li>
    </ul> 
  <li>Run the database migration</li>
  <li>Check your <code>schema.rb</code> if it contains the following</li>
  <pre><code class="ruby">
create_table "posts", force: :cascade do |t|
  t.string   "title"
  t.string   "author"
  t.text     "content"
  t.datetime "created_at", null: false
  t.datetime "updated_at", null: false
end
  </code></pre>
</ul>

<%= standard_git_workflow %>