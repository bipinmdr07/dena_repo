<h3>Submit your work</h3>
<p>
  Awesome work! Follow the directions on the lesson and submit your work. We'll get back to you soon and give you feedback or code review.
</p>

<p>
	(P.S Give it an awesome title name just for fun :D )
</p>

<%= simple_form_for :submission_comment, url: submission_submission_comments_path, method: :post, input_html: {class: 'form-horizontal'} do |f| %>
	<%= f.input :title, required: true %>
	<%= f.cktext_area :description, as: :text, input_html: {rows: 6}, required: true %>
	<%= f.input :lesson, :as => :hidden, :input_html => { :value => params[:lesson]} %>
	<%= f.input :submission_id, :as => :hidden, :input_html => { :value => params[:submission_id]} %>
	<%= f.submit 'Submit Question', class: 'btn btn-primary' %>
<% end %>